# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its graphics focus without claiming live deployment or external usage.

## Cases

- `baseline`: `geometry span`, score 230, lane `ship`
- `stress`: `atlas pressure`, score 173, lane `ship`
- `edge`: `shader drift`, score 214, lane `ship`
- `recovery`: `render budget`, score 170, lane `ship`
- `stale`: `geometry span`, score 264, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
